# 공용 레인은 @oube/release 패키지에 있다. 설치 경로가 앱마다 달라 require.resolve 로 찾는다.
package_json = `node -p "require.resolve('@oube/release/package.json')"`.strip
import File.join(File.dirname(package_json), "tools", "fastlane", "Fastfile")
